package factory.devices;

/**
 * The Server class. One of the NetworkDevice types.
 */
public class Server extends NetworkDevice{

    /**
     * Constructor for the class. Calls the super constructor function
     */
    public Server(String deviceId, String name) {
        super(deviceId, name);
    }
}
